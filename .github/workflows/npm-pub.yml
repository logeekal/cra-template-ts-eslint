name: NPM Publish

on: 
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: ubuntu-latest
    

    steps:
      - uses: actions/checkout@v1
      - name: Publish to npm
        uses: pascalgn/npm-publish-action@1.1.0
